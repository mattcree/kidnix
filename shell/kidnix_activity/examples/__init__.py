"""Example activities. Not installed on the image by default.

They exist so that the SDK's contract has something that keeps it, in the
repository, that CI runs -- an example nobody executes is documentation with a
bug in it. ``kidnix-shell --demo`` may list them; the shipped
``system_files/usr/share/kidnix/activities/`` deliberately does not.
"""

from __future__ import annotations
